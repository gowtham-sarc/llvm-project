import("//build_overrides/llvm.gni")

static_library("grammar") {
  output_name = "clangPseudoGrammar"
  configs += [ "//${llvm_build_root}build:clang_code" ]
  deps = [
    "//${llvm_root}clang/include/clang/Basic:AttrTokenKinds",
    "//${llvm_root}llvm/lib/Support",
  ]
  include_dirs = [ "../../include" ]
  sources = [
    "Grammar.cpp",
    "GrammarBNF.cpp",
    "LRGraph.cpp",
    "LRTable.cpp",
    "LRTableBuild.cpp",
  ]
}
