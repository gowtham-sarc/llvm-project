import("//build_overrides/llvm.gni")

executable("clang-pseudo-gen") {
  configs += [ "//${llvm_build_root}build:clang_code" ]
  deps = [
    "//${llvm_root}clang-tools-extra/pseudo/lib/grammar",
    "//${llvm_root}llvm/lib/Support",
  ]
  include_dirs = [ "../include" ]
  sources = [ "Main.cpp" ]
}
