import("//build_overrides/llvm.gni")

import("//${llvm_root}clang/resource_dir.gni")

source_set("sources") {
  configs -= [ "//${llvm_build_root}build:llvm_code" ]
  configs += [ "//${llvm_build_root}build:crt_code" ]
  sources = [ "cfi.cpp" ]
}

copy("ignorelist") {
  sources = [ "cfi_ignorelist.txt" ]
  outputs = [ "$clang_resource_dir/share/{{source_target_relative}}" ]
}
