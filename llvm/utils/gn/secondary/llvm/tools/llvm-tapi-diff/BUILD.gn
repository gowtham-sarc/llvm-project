import("//build_overrides/llvm.gni")

executable("llvm-tapi-diff") {
  deps = [
    "//${llvm_root}llvm/lib/Object",
    "//${llvm_root}llvm/lib/Support",
    "//${llvm_root}llvm/lib/TextAPI",
  ]
  sources = [
    "DiffEngine.cpp",
    "llvm-tapi-diff.cpp",
  ]
}
