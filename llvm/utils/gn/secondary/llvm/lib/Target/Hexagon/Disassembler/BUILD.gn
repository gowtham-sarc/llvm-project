import("//build_overrides/llvm.gni")

import("//${llvm_root}llvm/utils/TableGen/tablegen.gni")

tablegen("HexagonGenDisassemblerTables") {
  visibility = [ ":Disassembler" ]
  args = [ "-gen-disassembler" ]
  td_file = "../Hexagon.td"
}

static_library("Disassembler") {
  output_name = "LLVMHexagonDisassembler"
  deps = [
    ":HexagonGenDisassemblerTables",
    "//${llvm_root}llvm/lib/MC/MCDisassembler",
    "//${llvm_root}llvm/lib/Support",
    "//${llvm_root}llvm/lib/Target/Hexagon/MCTargetDesc",
    "//${llvm_root}llvm/lib/Target/Hexagon/TargetInfo",
  ]
  include_dirs = [ ".." ]
  sources = [ "HexagonDisassembler.cpp" ]
}
