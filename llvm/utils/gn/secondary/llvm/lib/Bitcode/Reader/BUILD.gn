import("//build_overrides/llvm.gni")

static_library("Reader") {
  output_name = "LLVMBitReader"
  deps = [
    "//${llvm_root}llvm/include/llvm/Config:llvm-config",
    "//${llvm_root}llvm/lib/Bitstream/Reader",
    "//${llvm_root}llvm/lib/IR",
    "//${llvm_root}llvm/lib/Support",
    "//${llvm_root}llvm/lib/TargetParser",
  ]

  sources = [
    "BitReader.cpp",
    "BitcodeAnalyzer.cpp",
    "BitcodeReader.cpp",
    "MetadataLoader.cpp",
    "ValueList.cpp",
  ]
}
