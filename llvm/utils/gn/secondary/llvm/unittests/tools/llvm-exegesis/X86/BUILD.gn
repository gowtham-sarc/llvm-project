import("//build_overrides/llvm.gni")

import("//third-party/unittest/unittest.gni")

unittest("LLVMExegesisX86Tests") {
  deps = [
    "//${llvm_root}llvm/lib/DebugInfo/Symbolize",
    "//${llvm_root}llvm/lib/MC",
    "//${llvm_root}llvm/lib/MC/MCParser",
    "//${llvm_root}llvm/lib/Object",
    "//${llvm_root}llvm/lib/Support",
    "//${llvm_root}llvm/lib/Target/X86",

    # Exegesis reaches inside the Target/X86 tablegen internals and must
    # depend on this Target/X86-internal build target -- and so must its
    # unittests.
    "//${llvm_root}llvm/lib/Target/X86/MCTargetDesc",
    "//${llvm_root}llvm/tools/llvm-exegesis/lib",
    "//${llvm_root}llvm/tools/llvm-exegesis/lib/X86",
  ]
  include_dirs = [
    "..",
    "//${llvm_root}llvm/lib/Target/X86",
    "//${llvm_root}llvm/tools/llvm-exegesis/lib",
  ]
  sources = [
    "BenchmarkResultTest.cpp",
    "RegisterAliasingTest.cpp",
    "SchedClassResolutionTest.cpp",
    "SnippetFileTest.cpp",
    "SnippetGeneratorTest.cpp",
    "SnippetRepetitorTest.cpp",
    "SubprocessMemoryTest.cpp",
    "TargetTest.cpp",
  ]
}
