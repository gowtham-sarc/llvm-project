import("//build_overrides/llvm.gni")

import("//${llvm_build_root}build/libs/terminfo/enable.gni")

config("terminfo_config") {
  visibility = [ ":terminfo" ]
  libs = [ "ncurses" ]
}

group("terminfo") {
  if (llvm_enable_terminfo) {
    public_configs = [ ":terminfo_config" ]
  }
}
